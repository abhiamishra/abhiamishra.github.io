# notes that i learnt while creating my homepage

### serverside v clientside rendering
- server side has everything render on the server and is the preview that SEOs can access
- client side happens on the brower as everything is loaded and packaged there
- we can have components that are server side w/ only the client side components being inserted into those.